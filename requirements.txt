# Pinned to address CVE-2026-24049 and CVE-2026-23949
setuptools>=78.0.0
wheel>=0.46.2
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.13.0
certifi==2026.2.25
click==8.3.2
distro==1.9.0
docstring_parser==0.18.0
fastapi==0.135.3
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.11
jiter==0.14.0
packaging==26.1
pydantic==2.13.1
pydantic_core==2.46.1
python-dotenv==1.2.2
sniffio==1.3.1
# 1.0.0 carried CVE-2026-48818 (SSRF via UNC paths in StaticFiles) and
# CVE-2026-54283 (form() limits ignored -> DoS). fastapi only needs >=0.46.0.
starlette==1.6.0
typing-inspection==0.4.2
typing_extensions==4.15.0
uvicorn==0.44.0

# RAG. langchain-classic/community/experimental went with the retrievers that
# needed them (EnsembleRetriever, BM25Retriever, SemanticChunker).
langchain==1.3.1
langchain-anthropic==1.4.3
langchain-chroma==1.1.0
langchain-openai==1.2.2
langchain-text-splitters==1.1.2
chromadb==1.5.9
# Sparse half of the hybrid retriever, built per-user.
rank_bm25==0.2.2

# Datasource providers (packages/core/storage). boto3 talks to LocalStack in
# compose and to real S3 unchanged - only the endpoint URL differs.
boto3==1.40.47

# Dev-login JWTs. Identity is read from the verified token only, never from a
# request body or header.
PyJWT==2.10.1

# Tests. moto stands in for S3 so the real boto3 provider code is exercised in
# CI without Docker or a LocalStack container - the alternative was a hand
# written fake that would have tested everything except the code that talks to
# the provider.
pytest==8.4.2
moto[s3]==5.1.14
